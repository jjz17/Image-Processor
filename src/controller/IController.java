package controller;

/**
 * Represents a controller to interact with a {@code IImageProcessorModel}.
 */
public interface IController {
  /**
   * Allows the user to use the controller to interact with the processor.
   */
  void useProcessor();
}
